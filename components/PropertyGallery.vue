<template>
<div class="app-section">
    <div class="app-wrapper">
        <div class="app-masonry">
            <div v-for="publicId in images" 
            :key="publicId" 
            :style="`
            background-image: url(${getImageUrl(publicId)}); 
            background-repeat: no-repeat; 
            background-size:auto 100%; 
            padding:100px; 
            border-radius: 24px;`">
                
            </div>
        </div>
    </div>
</div>
</template>
<script>
export default {
    props: {
        images:{
            type: Array,
            requierd: true,
        }
    },
    methods:{
        getImageUrl(publicId){
            return this.$img(publicId, {
                width: 600,
            }, {
                provider: 'cloudinary'
            })

        }
    }
}
</script>